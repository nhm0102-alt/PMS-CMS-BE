spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://pms-mysql.cf0wuyik4tx2.ap-southeast-1.rds.amazonaws.com:3306/pms?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${DB_USERNAME:pms_app}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}

app:
  jwt:
    secret: ${APP_JWT_SECRET}
